# GPT2/SMALL
name = OpenAI GPT-2 SMALL (124M)
transformer.type = OPENAI_GPT_2

# Main dimensions
hidden.size = 768
feedforward.size = 3072
decoder.count = 12
attention.head.count = 12
epsilon = 1e-5f

# Tokenizer config
tokenizer = GPT-2
tokenizer.config = GPT-2
token.count = 50257
end.of.text.token = 50256
max.length = 1024

# Parameter config
parameter.url =
parameter.files = model.safetensors
transformer.parameter.format = {name}
decoder.parameter.format = h.{decoderId}.{name}
transformer.parameter.overrides =
decoder.parameter.overrides =